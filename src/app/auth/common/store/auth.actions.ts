// import { createAction, props } from '@ngrx/store';
// import { User } from '../models/user';

// export const loadUser = createAction('[Login Component] Load user');

// export const userLoaded = createAction('[Load Auth Effect] User Loaded', props<{ user: User }>());

// export const updateUser = createAction('[ProfileContactStep Component] Update User', props<{ user: User }>());

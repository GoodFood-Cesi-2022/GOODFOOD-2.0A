export enum Endpoints {
    SECURE_USER='/api/secure/user',
    PROFILE = '/api/profile',
    LOGOUT = '/api/logout'
}

export enum Roles {
    ADMINISTRATOR = 'admin',
    MANAGER = 'manager',
    FRANCHISEE = 'franchisee'
}